// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  authentification_api_url: 'http://localhost:8080',
  conservatoire_user_api_url: 'http://localhost:8081',
  time_management_api_url: 'http://localhost:8082',
  authentification_application_url: 'http://localhost:4200',
  auth_token: 'e7866875-ea30-4b62-9ad9-3def58c7eed4'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
